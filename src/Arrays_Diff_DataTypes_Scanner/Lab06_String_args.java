//Also, if you print args[0] or args[1] or agrs.length like below, it will throw ArrayIndexOutOfBoundsException.
//You can even assign args by clicking on three dots, then args[0], args.length will work.

package Arrays_Diff_DataTypes_Scanner;

public class Lab06_String_args {
    public static void main(String[] args) {
        System.out.println(args[0]);
        System.out.println(args[1]);
        System.out.println(args.length);
    }
}
