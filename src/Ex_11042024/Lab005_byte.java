package Ex_11042024;
//byte can only store value from -128 to 127. It cannot store alphabets / special characters.
//byte variable can start with lowercase/uppercase alphabets, underscore(_) and dollar(#).
//byte variable contains integer, lowercase/uppercase alphabets, underscore (_) and dollar($).
//byte variable cannot start with an integer.
//Commented one's are not allowed.
//%d is used for byte.

public class Lab005_byte {
    public static void main(String[] args) {
        //byte 12=0;
        //byte @=128;
        byte $2=127;
        //byte _$=0;
        System.out.printf("The byte is %d", $2);
        //System.out.println(_$);
    }
}
